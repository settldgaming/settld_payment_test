{
  "name": "settld_test",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "echo \"Building static assets\"",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^5.1.0"
  }
}
